{
  "name": "aether",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "AETHER — deep-zoom fractal explorer (perturbation + arbitrary-precision reference orbits)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
